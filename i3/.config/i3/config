set $mod Mod4
font Hack Nerd Font 14

default_border pixel 2
default_floating_border pixel 2

exec --no-startup-id sh $HOME/.config/scripts/startup.sh

#rules
for_window [class="^.*"] border pixel 0
for_window [class="Slack"] move container to workspace 5
for_window [class="Signal"] move container to workspace 5
for_window [class="Pavucontrol"] floating enable

# my bindings
bindsym $mod+w exec google-chrome-stable
bindsym $mod+Return exec alacritty
bindsym $mod+s exec flameshot gui
bindsym $mod+plus exec sh $HOME/.config/scripts/redshift.sh up
bindsym $mod+minus exec sh $HOME/.config/scripts/redshift.sh down
bindsym $mod+space exec --no-startup-id rofi -combi-modi window#drun#ssh -theme Monokai -font "Hack Nerd Font 20" -show combi -icon-theme "Papirus" -show-icons
bindsym $mod+bracketright exec sh $HOME/.config/scripts/volume.sh up
bindsym $mod+bracketleft exec sh $HOME/.config/scripts/volume.sh down
bindsym $mod+p exec sh $HOME/.config/scripts/volume.sh mute


# my workspaces
workspace 1 output HDMI-1 eDP-1
workspace 2 output HDMI-1 eDP-1
workspace 3 output HDMI-1 eDP-1
workspace 4 output HDMI-1 eDP-1
workspace 5 output DP-1 eDP-1
workspace 6 output DP-1 eDP-1
workspace 7 output DP-1 eDP-1
workspace 8 output DP-1 eDP-1

# laptop controls
bindsym XF86AudioRaiseVolume exec sh $HOME/.config/scripts/volume.sh up
bindsym XF86AudioLowerVolume exec sh $HOME/.config/scripts/volume.sh down
bindsym XF86AudioMute exec sh $HOME/.config/scripts/volume.sh mute
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle
bindsym XF86MonBrightnessUp exec brightnessctl s 15+
bindsym XF86MonBrightnessDown exec brightnessctl s 15-

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# kill focused window
bindsym $mod+c kill

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+u split v

# split in vertical orientation
bindsym $mod+v split h

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

bindsym $mod+e layout toggle split

bindsym $mod+Shift+space floating toggle

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8

# reload the configuration file
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# resize window (you can also use the mouse for that)
mode "resize" {
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"


